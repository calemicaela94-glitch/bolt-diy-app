name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: Install dependencies
        run: ELECTRON_SKIP_BINARY_DOWNLOAD=1 pnpm install --no-frozen-lockfile
        env:
          ELECTRON_SKIP_BINARY_DOWNLOAD: "1"

      - name: Build web app (SPA mode for APK)
        run: pnpm remix vite:build --config vite.config.spa.ts

      - name: Create www folder for Capacitor
        run: |
          mkdir -p www
          cp -r build/client/* www/

          # Verify index.html exists (generated by Remix SPA build)
          if [ ! -f www/index.html ]; then
            echo "ERROR: index.html not found in build/client!"
            echo "Available files in build/client:"
            ls build/client/ 2>/dev/null
            exit 1
          fi

          echo "=== Generated index.html (from Remix SPA build) ==="
          head -30 www/index.html
          echo "... (truncated)"
          echo "===================================================="

      - name: Setup Java 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Install Capacitor and Android platform
        run: |
          npm install -g @capacitor/cli
          npm install @capacitor/core @capacitor/android

      - name: Build APK with Capacitor
        run: |
          npx cap init Bolt.diy ai.bolt.app --web-dir=www
          npx cap add android
          npx cap sync

          # Forçar versão do Kotlin para resolver conflitos
          echo "subprojects {
              project.configurations.all {
                  resolutionStrategy {
                      force 'org.jetbrains.kotlin:kotlin-stdlib:1.8.22'
                      force 'org.jetbrains.kotlin:kotlin-stdlib-jdk7:1.8.22'
                      force 'org.jetbrains.kotlin:kotlin-stdlib-jdk8:1.8.22'
                  }
              }
          }" >> android/build.gradle

          cd android && chmod +x gradlew && ./gradlew assembleDebug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: bolt-app
          path: android/app/build/outputs/apk/debug/*.apk